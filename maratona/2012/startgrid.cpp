#include <bits/stdc++.h>

using namespace std;

int main()
{

    int n;

    while (cin >> n)
    {

        vector<int> vet(n), vet2(n);

        for (int i = 0; i < n; i++)
        {
            cin >> vet[i];
        }

        for (int i = 0; i < n; i++)
        {
            cin >> vet2[i];
        }

        int swaps = 0;
    }
    return 0;
}